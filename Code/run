# !/bin/sh

echo "Running..."
if [ "$1" = "install" ]; then
  echo "Installing dependencies..."
  cd src
  echo "In src folder"
  npm install
  tsc index.ts && node index.js
  echo "dependencies installed"
  exit 0
else
  python ../main.py "$@"
  exit 0
fi

exit 0